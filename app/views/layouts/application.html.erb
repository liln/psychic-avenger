<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Lillian Ng" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <%= javascript_include_tag "application" %>

  </head>

  <body>
<a name="#top"></a>
<nav>

     <div class="twelve columns header_nav">
     <div class="row">

        <ul id="menu-header" class="nav-bar horizontal">

          <li class=""><a href="<%= root_path %>">Home</a></li>
          <li class=""><a href="<%= root_path %>#aboutme">About Me</a></li>
          <li class=""><a href="<%= projects_path %>">Projects</a></li>
          <li class=""><a href="<%= posts_path %>">Blog</a></li>
          <li class=""><a href="#connect">Connect</a></li>
        </ul>

        <script type="text/javascript">
         //<![CDATA[
         $('ul#menu-header').nav-bar();
          //]]>
        </script>

      </div>
      </div>

</nav>

  <% if flash.empty? == 0 || notice.nil? == 0 || alert.nil? == 0 %>
    <section class="section_light">
      <div class="twelve columns">
        <% flash.each do |key, value| %>
          <div class="alert-box round <%= flash_class(key) %>">
            <%= value %>
          </div>
        <% end %>
        <% if notice.nil? == 0 %>
          <p class="notice"><%= notice %></p>
        <% end %>
        <% if alert.nil? == 0 %>
          <p class="alert"><%= alert %></p>
        <% end %>
      </div>
    </section>
  <% end %>



    <%= yield %>

<!-- ######################## Section ######################## -->

    <section class="section_light">

          <div class="twelve columns">
            <h3 style="text-align:center"><a name="connect"></a>Connect</h3>
            <p class="text-center">
              <a href="http://twitter.com/Lillian_Ng" class="lsf" style="font-size:16px; margin-right:15px" title="twitter">Twitter</a>
              <a href="http://github.com/orangeninjamidget" class="lsf" style="font-size:16px; margin-right:15px" title="github">Github</a>
              <a href="http://linkedin/in/lilliankng" class="lsf" style="font-size:16px; margin-right:15px" title="linkedin">LinkedIn</a>
            </p>
          </div>

    </section>

<!-- ######################## Footer ######################## -->

<footer>

      <div class="row">

          <div class="six columns footer">
            Copyright &copy; 2014. Lillian Ng
          </div>

          <div class="six columns footer">
          <% if current_user.present? %>
            Signed in as <%= current_user.email %>.
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
          <% else %>
            <%= link_to "Sign Up", new_user_registration_path %>
            <%= link_to "Sign In", new_user_session_path %>
          <% end %>
          </div>

      </div>

</footer>

  </body>
</html>
